spring:
  application:
    name: config-server
  cloud:    config:      server:        git:          uri: c:\\Workspace\\config-server-repo          clone-on-start: true
server:  port: 8888
